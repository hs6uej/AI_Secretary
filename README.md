# AIS AI Secretary - Frontend

<div align="center">
  <img src="./src/components/layout/logoais.png" alt="AIS AI Secretary Logo" width="200"/>
</div>

<p align="center">
  <strong>ส่วนติดต่อผู้ใช้ (Frontend) สำหรับแอปพลิเคชัน AI Secretary</strong>
</p>
<p align="center">
  สร้างขึ้นด้วย React (Vite), TypeScript, และ Tailwind CSS
</p>
<p align="center">
  <img src="https://img.shields.io/badge/React-18-blue?logo=react" alt="React">
  <img src="https://img.shields.io/badge/Vite-5-purple?logo=vite" alt="Vite">
  <img src="https://img.shields.io/badge/TypeScript-5-blue?logo=typescript" alt="TypeScript">
  <img src="https://img.shields.io/badge/Tailwind_CSS-3-blue?logo=tailwindcss" alt="Tailwind CSS">
</p>

---

## ✨ เกี่ยวกับโปรเจกต์ (About This Project)

โปรเจกต์นี้คือส่วน Frontend ของระบบ **AI Secretary** ซึ่งเป็น Dashboard สำหรับผู้ใช้งานในการจัดการและตรวจสอบการทำงานของเลขาส่วนตัว AI ประกอบไปด้วยระบบยืนยันตัวตน, หน้าแดชบอร์ดสรุปผล, ประวัติการโทร, การจัดการรายชื่อ, และการตั้งค่าต่างๆ

## 🛠️ เทคโนโลยีที่ใช้ (Tech Stack)

* **Framework:** [React 18](https://reactjs.org/)
* **Bundler:** [Vite 5.x](https://vitejs.dev/)
* **Language:** [TypeScript 5.x](https://www.typescriptlang.org/)
* **Styling:** [Tailwind CSS 3.x](https://tailwindcss.com/)
* **Routing:** [React Router v6](https://reactrouter.com/)
* **Data Fetching:** [Axios](https://axios-http.com/)
* **Icons:** [Lucide React](https://lucide.dev/)
* **Utilities:** [date-fns](https://date-fns.org/)

## 🚀 คุณสมบัติหลัก (Core Features)

* **🔐 ระบบยืนยันตัวตน:** หน้า Login และ Register พร้อมระบบ Bypass สำหรับทดสอบ
* **📊 แดชบอร์ด:** ภาพรวมสถานะ (Available, DND), สรุปสายที่ไม่ได้รับ, สแปมที่ถูกบล็อก และ Quick Actions
* **📞 ประวัติการโทร:** รายการสายเรียกเข้าทั้งหมด พร้อมตัวกรองสถานะ (Missed, Spam) และเวลา
* **🎧 รายละเอียดการโทร:** ดูสรุปการสนทนา, outcome, และฟังเสียงบันทึก (Audio Playback)
* **👥 การจัดการรายชื่อ:** เพิ่ม ลบ แก้ไข รายชื่อ และตั้งค่า Whitelist/Blacklist
* **⚙️ การตั้งค่า:** จัดการโปรไฟล์ (ชื่อ, คำอ่าน), ตั้งค่า Announcement และ Do Not Disturb (DND)

---

## 🏁 การติดตั้งและเริ่มต้นใช้งาน (Getting Started)

### 1. ข้อกำหนดเบื้องต้น (Prerequisites)

* [Node.js](https://nodejs.org/) (เวอร์ชัน 18.x ขึ้นไป)
* [Yarn](https://yarnpkg.com/) (เนื่องจากโปรเจกต์นี้มีไฟล์ `yarn.lock`)

### 2. โคลนโปรเจกต์ (Clone the Repository)

```bash
git clone <your-repository-url>
cd AI_Secretary-f04fdff01b41b17fad6908373f4cc6f2c2a30da6
3. ติดตั้ง Dependencies (Install Dependencies)
ใช้ Yarn (ตาม yarn.lock) เพื่อติดตั้งแพ็คเกจที่จำเป็นทั้งหมด:

Bash

yarn install
4. ❗ การตั้งค่า Backend (Backend Configuration)
โปรเจกต์นี้ถูกตั้งค่าให้สื่อสารกับ Backend API ผ่าน Proxy เพื่อหลีกเลี่ยงปัญหา CORS

การตั้งค่าปัจจุบัน (ใน vite.config.ts): แอปพลิเคชันจะพยายามเชื่อมต่อกับ API ที่ http://192.168.38.46:5028

TypeScript

// vite.config.ts
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// [https://vitejs.dev/config/](https://vitejs.dev/config/)
export default defineConfig({
  plugins: [react()],
  server: {
    proxy: {
      '/api': {
        target: '[http://192.168.38.46:5028](http://192.168.38.46:5028)', // <--- แก้ไขที่บรรทัดนี้
        changeOrigin: true,
      },
    },
  },
})
คุณต้อง:

ตรวจสอบให้แน่ใจว่า Backend Server ของคุณกำลังทำงานอยู่ที่ http://192.168.38.46:5028

หรือ เปลี่ยนค่า target ในไฟล์ vite.config.ts ให้ตรงกับที่อยู่ของ Backend Server ของคุณ

5. รันโปรเจกต์ (Run the Project)
หลังจากตั้งค่า Backend Proxy เรียบร้อยแล้ว สั่งรัน Development Server:

Bash

yarn dev
แอปพลิเคชันจะเปิดที่ http://localhost:5173 (หรือ Port อื่นหาก 5173 ไม่ว่าง)

📜 สคริปต์ที่ใช้งานได้ (Available Scripts)
ในไฟล์ package.json คุณจะพบสคริปต์หลักๆ ดังนี้:

yarn dev: รันแอปพลิเคชันในโหมด Development พร้อม Hot-Reload

yarn build: คอมไพล์และบิวด์โปรเจกต์สำหรับ Production ไปยังโฟลเดอร์ dist/

yarn lint: รัน ESLint เพื่อตรวจสอบข้อผิดพลาดและสไตล์ของโค้ดในไฟล์ .js, .jsx, .ts, .tsx

yarn preview: รัน Production Build (จาก dist/) บน Local Server เพื่อทดสอบก่อน Deploy จริง

📂 โครงสร้างโปรเจกต์ (Project Structure)
ai_secretary/
├── public/               # ไฟล์สแตติก (ไม่ได้ใช้งานในโปรเจกต์นี้)
├── src/
│   ├── components/       # Components ที่ใช้ซ้ำ
│   │   ├── dashboard/    # Components สำหรับหน้า Dashboard
│   │   ├── layout/       # Components โครงสร้าง (Sidebar, Navbar, Layout)
│   │   └── ui/           # UI Components พื้นฐาน (Button, Input)
│   ├── context/          # React Contexts (AuthContext, ThemeContext)
│   ├── pages/            # Page Components (สำหรับ React Router)
│   ├── services/         # Logic การเชื่อมต่อ API (axios, auth, calls)
│   ├── types/            # Type definitions (user.ts, call.ts, contact.ts)
│   ├── App.tsx           # Main App Component (รวม Provider)
│   ├── AppRouter.tsx     # ตัวจัดการ Routes (Public/Protected)
│   ├── index.css         # Tailwind CSS imports
│   └── index.tsx         # จุดเริ่มต้นของแอป (React DOM)
├── .eslintrc.cjs         # กฎของ ESLint
├── .gitignore            # ไฟล์ที่ Git จะข้าม
├── package.json          # รายการ Dependencies และ Scripts
├── postcss.config.js     # การตั้งค่า PostCSS (สำหรับ Tailwind)
├── tailwind.config.js    # การตั้งค่า Tailwind CSS (Colors, Fonts)
├── tsconfig.json         # การตั้งค่า TypeScript
├── vite.config.ts        # การตั้งค่า Vite (รวมถึง Proxy)
└── yarn.lock             # ไฟล์ Lock สำหรับ Yarn
